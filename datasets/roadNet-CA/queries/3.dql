{
  var(func: has(id)) {
    degree as count(successors)
  }

  nodes as var(func: eq(val(degree), 3)) @cascade(successors) {
    successors @filter(eq(id, 562818))
  }

  result(func: uid(nodes)) {
    uid
  }
}
